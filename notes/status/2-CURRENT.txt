CURRENT — 2026-07-07 (Claude Code, cloud)

GrindNotes now lives in this repo (Salinator513/Notes) — built from the grindquest
tools/grindnotes base since the local install isn't reachable from the cloud session.

This turn:
- Cursor: full 360° spins now possible (no angle clamp; hard flick loops, then gravity
  drops it back and it settles). Physics in cursor.js, shared by both pages.
- Usage window reworked: one card per prompt — prompt title, date in little letters on
  the right, then just usage +Δ% and context +Δ%. No weekly block, no description line.
- Cowork removed from the entire app: prompt targets are Code + Codex only; usage tabs
  are Claude Code / Codex; cowork-sourced usage entries are never shown.
- Turn summaries: scrolling list, ~3 visible at a time.
- Server on port 7718 with /api/delete (scratch notes, moved to notes/.trash, ✕ on hover).
- Bug fixes: failed saves retry instead of silently dropping edits, unload saves via
  keepalive, path-prefix check hardened, missing-file opens no longer blank the app.

"Can't be reached" fix (same day): GrindNotes becomes a live website — a read-only
mirror is deployed to the gh-pages branch on every push, serving at
https://salinator513.github.io/Notes/ (status, turn summaries, usage, cursor all work;
editing needs the local server). ONE manual step remains (admin-only, agents can't):
repo Settings → Pages → Source "Deploy from a branch" → gh-pages / root → Save.
The localhost link only works while the server runs on the PC: pull the branch,
double-click GrindNotes.bat — the launcher now checks for Node, waits for the server,
opens the right port, and shows the error instead of vanishing. If 7718 is taken it
hops to the next port; --lan mode reaches it from a phone on the same wifi.

Awaiting Sasha: cursor feel check (spin + settle), usage panel verdict.
