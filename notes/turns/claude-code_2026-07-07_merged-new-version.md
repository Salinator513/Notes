The new GrindNotes — local design + cloud functionality merged, cowork gone

- Local agent delivered the real local app (branch codex/local-grindnotes-current);
  repo root now carries the combined version: orange grouped-Sets design leading,
  cloud functionality underneath.
- Cursor spins full 360° on hard flicks (verified 4 wrap-arounds headless), settles
  like a heavy pendulum; cowork stripped from the entire app per Sasha.
- Usage cards: prompt title + small date right + Usage/Context deltas only.
- Same ui.html serves locally AND as the live Pages mirror (read-only fallback);
  delete goes to notes/.trash; server hops busy ports; launcher fails loud.
- Local install update still manual: copy the 5 app files over, never notes/.
