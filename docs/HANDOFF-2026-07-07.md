# HANDOFF — cloud session → local desktop session (2026-07-07)

You are taking over GrindNotes work from a Claude Code cloud session. Read this
whole file, then `CLAUDE.md` (repo root), then `notes/status/2-CURRENT.txt`.

## What this project is

GrindNotes: Sasha's zero-dependency notes hub for the GRINDWAVE / GRINDQUEST work.
One Node server (`server.mjs`), two pages (`ui.html`, `usage.html`), data in
`notes/`. No packages, no build step — keep it that way. This repo became the
CANONICAL app on 2026-07-07: it is the merge of Sasha's local orange redesign
(grouped note Sets, prompt dock, corner Usage/Type rail, collapsible sidebar,
custom swinging cursor) with the cloud rebuild's functionality (full list in
CLAUDE.md "History" section). Sasha approved a public read-only mirror: every push
to `claude/cursor-swing-physics-bstakk` auto-deploys to
https://salinator513.github.io/Notes/ via `.github/workflows/pages.yml` →
`gh-pages`. Remember: pushes here are public within minutes.

## State at handoff

- Branch `claude/cursor-swing-physics-bstakk` (repo default) has everything:
  merged app, Pages workflow, updater, chatlog/status/turn-note housekeeping.
- Note text (view + editor) uses Notepad's font per Sasha: Consolas 11pt,
  regular weight (`#view`/`#edit` in ui.html). App chrome stays Nunito.
- All verified headless: boot, grouped Sets, delete→`notes/.trash`, usage cards,
  static mirror mode, cursor physics (4 full 360° wrap-arounds on a hard flick,
  settles to ~0° like a heavy pendulum).
- **Sasha's local install still runs the OLD version.** That's your first job.
- The Pages mirror already serves the new version.

## First tasks, in order

1. **Update the install**: replace `ui.html`, `usage.html`, `server.mjs`,
   `GrindNotes.bat`, `GrindNotes-App.vbs` in
   `C:\Users\Sasha\Documents\Codex\2026-07-06\i-wa\outputs\GrindNotes`
   with this repo's root versions. **NEVER touch the install's `notes\` folder —
   that's his data.** Relaunch `GrindNotes.bat` (opens an app window on
   http://127.0.0.1:7717/, hops ports if busy).
2. **Feel check with Sasha**: hard mouse flick should spin the cursor full
   circles then settle calmly; slow moves = gentle lean. Usage window (corner
   rail → Usage): one card per prompt — title, small date right, Usage/Context
   deltas, no weekly, no descriptions. He judges by feel; iterate. Cursor knobs
   live in `initCursor()` in `ui.html`: kick gain .45 (clamp ±28), gravity 1.2,
   drag .95 spinning / .86 settling (threshold 12), vel cap ±45.
3. **Chatlog sync**: this repo's `docs/CHATLOG-SYNC-2026-07-07.md` holds the
   verbatim log the cloud session couldn't deliver to grindquest. Append its
   entries to `grindquest/CHATLOG-GRINDWAVE.md` (repo Salinator513/grindquest),
   push, then delete the sync file here and push that too.
4. Optional, ask Sasha first: retire the stale `grindquest/tools/grindnotes/`
   copy (point its INSTALL doc at this repo).

## Standing rules (do not drop these)

- **Log verbatim**: every user message + assistant response, typos included, into
  `grindquest/CHATLOG-GRINDWAVE.md` (you're local — you can push there).
- **Turn summaries**: ≤5 bullets to the INSTALL's `notes\turns\`
  (`claude-code_<date>_<topic>.md`, first line = short English title) + usage
  entry to the install's `notes\usage.json`
  (`{ts, source:"claude-code", context_pct, plan_pct, note}` — note = prompt
  title, it's what the Usage window shows). Keep the repo's `notes/` roughly in
  sync when you touch the app (it feeds the public mirror).
- **No Cowork anywhere** (Sasha, 2026-07-07): targets Code + Codex, tabs
  Claude / Codex. Old cowork-era files fall back to the Code chip.
- **Zero dependencies**, no build step.
- Sasha judges by feel — ship, let him try, expect iteration.

## Gotchas that bit us today (don't repeat them)

- There were TWO GrindNotes (local install vs repo). The merge ended that: THIS
  REPO WINS now, and the install is updated FROM it. The pre-merge local source
  is archived in `local-grindnotes-current/` for reference.
- The install's `notes\` folder is sacred — app files only, never data.
- `Update-GrindNotes.bat` (repo root, also served on the mirror) is the
  user-facing updater: Sasha can double-click it to pull the latest app files.
  If you change file names/layout, update it too.
- The GitHub Pages URL serves whatever `tools/build-site.mjs` puts in `dist/` —
  if you add app assets, add them to that script or the mirror silently misses
  them (the ui/usage pages fall back to `notes/index.json` snapshot mode when
  `/api` is absent; keep that working).

## Where everything is

- This repo: https://github.com/Salinator513/Notes (branch above; public).
- Live mirror: https://salinator513.github.io/Notes/
- Project hub / chatlogs / goals: Salinator513/grindquest repo.
- Jukebox (separate GRINDWAVE project): Salinator513/grindwave-jukebox +
  its own CLAUDE.md pointing at grindquest.
